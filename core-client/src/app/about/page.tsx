import type { Metadata } from "next";
import Link from "next/link";
import {
  AUTO_MODEL_ID,
  MODELS,
  getModelCapabilityLabel,
} from "@/config/models";

export const metadata: Metadata = {
  title: "О Hermes — AI-чат с открытыми моделями",
  description:
    "Как Hermes подключает бесплатные LLM через OpenRouter и какие лицензии использует.",
};

const auditedModels = MODELS.filter((model) => model.id !== AUTO_MODEL_ID);

export default function AboutPage() {
  return (
    <div className="about-document-page">
      <header className="about-document-header">
        <Link href="/">Hermes</Link>
        <Link href="/">Чат</Link>
      </header>

      <main className="about-document-main">
        <header className="about-document-intro">
          <h1>О Hermes</h1>
          <p>Как мы собираем в одном чате проверенные языковые модели.</p>
        </header>

        <article className="about-document-body">
          <p>
            Hermes — это интерфейс для LLM-чатов. Мы не обучаем свою модель и не
            выдаём чужие модели за свои. Hermes даёт единый интерфейс, хранит историю
            диалога и выбирает резервную модель, если основной провайдер временно не отвечает.
          </p>

          <h2>Коротко</h2>
          <ul>
            <li>Один интерфейс для обычного диалога, кода, анализа текста, фото и видео.</li>
            <li>Только конкретные модели, которые есть в OpenRouter и прошли реальный API-тест.</li>
            <li>Серверный API-ключ: он не передаётся в браузер и не показывается пользователю.</li>
            <li>Потоковые ответы, повторные попытки и резервный маршрут при временном сбое провайдера.</li>
          </ul>

          <h2>Что делает Hermes</h2>
          <p>
            Hermes объединяет модели с разными возможностями в одном чате. Одни лучше подходят для
            фото и видео, другие — для кода, длинного контекста или сложного рассуждения. Выбор модели
            закрепляется за диалогом, чтобы поведение чата не менялось незаметно.
          </p>
          <p>
            Помимо доставки запроса Hermes готовит контекст, передаёт нужные вложения, показывает
            ответ по мере его генерации и сохраняет историю так, чтобы к разговору можно было вернуться.
          </p>

          <h2>Как проходит запрос</h2>
          <ol>
            <li>Браузер отправляет текст, часть истории и нужные вложения на сервер Hermes.</li>
            <li>Сервер проверяет модель и формат запроса, затем обращается к OpenRouter со своим API-ключом.</li>
            <li>OpenRouter выбирает доступный хостинг конкретной модели и передаёт ему запрос.</li>
            <li>Ответ возвращается потоком; если до начала ответа возник временный сбой, Hermes может повторить запрос или сменить модель.</li>
          </ol>

          <h2>История и данные</h2>
          <p>
            Гостевая история хранится в браузере. У авторизованных пользователей текстовая история
            может синхронизироваться между устройствами. Файлы и кадры видео передаются модели только тогда,
            когда они нужны для текущего запроса.
          </p>
          <p>
            OpenRouter не логирует тексты запросов и ответов по умолчанию,
            но у каждого конечного провайдера есть свои правила. Поэтому в чат не стоит отправлять
            пароли, ключи и другие секреты. Подробнее — в
            {" "}
            <a href="https://openrouter.ai/docs/guides/privacy/provider-logging" target="_blank" rel="noreferrer">
              политике OpenRouter
            </a>.
          </p>

          <h2>Как работает Auto</h2>
          <p>
            Auto — это локальная логика Hermes, а не отдельная модель и не динамический каталог OpenRouter. Для текста
            и мультимодальных запросов заданы разные короткие списки резервных моделей. Модель без поддержки
            фото или видео не попадает в маршрут такого запроса.
          </p>

          <h2>Модели</h2>
          <p>
            В каталоге остаются только конкретные версии. Мы проверяем наличие модели в OpenRouter, нулевую цену,
            форматы входа и обычный текстовый ответ через тот же API, который использует чат.
          </p>
          <ul aria-label="Проверенные модели">
            {auditedModels.map((model) => (
              <li key={model.id}>
                <strong>{model.title}</strong> — {model.description} {model.provider}, {getModelCapabilityLabel(model).toLowerCase()},
                {" "}
                {model.license ? (
                  <a href={model.license.url} target="_blank" rel="noreferrer">
                    {model.license.name}
                  </a>
                ) : null}.
              </li>
            ))}
          </ul>

          <h2>Бесплатность и лимиты</h2>
          <p>
            На 16 августа 2026 года все перечисленные версии имеют нулевую цену входных и выходных токенов
            в OpenRouter. Бесплатная цена не означает неограниченную мощность: OpenRouter и провайдеры могут менять дневные
            лимиты, очереди и доступность без предупреждения. Поэтому Hermes не обещает бесконечное число запросов.
          </p>

          <h2>Лицензии</h2>
          <p>
            Hermes получает ответы через API и не распространяет веса моделей. Веса остаются у провайдеров, а Hermes
            использует их удалённо. Если кто-то распространяет сами веса или их изменённые версии, Apache 2.0, OpenMDW и лицензии
            NVIDIA требуют сохранять положенные notices, копии лицензий и указания на исходное происхождение. Прямые ссылки на лицензии
            указаны рядом с каждой моделью выше.
          </p>

          <h2>Ограничения</h2>
          <p>
            Ответ модели может быть неточным, неполным или устаревшим. Hermes не заменяет профессиональную
            медицинскую, юридическую или финансовую проверку. В задачах с высокой ценой ошибки ответ нужно перепроверять
            по надёжным первичным источникам.
          </p>

          <h2>Актуальность</h2>
          <p>
            Доступность, лимиты и цены бесплатных моделей могут меняться.
            Каталог проверен <time dateTime="2026-08-16">16.08.2026</time>. Текущий список есть в
            {" "}
            <a href="https://openrouter.ai/models" target="_blank" rel="noreferrer">
              каталоге OpenRouter
            </a>.
          </p>
        </article>
      </main>
    </div>
  );
}
