import type { Metadata } from "next";

export const metadata: Metadata = { title: "Get help — Hermes" };

export default function HelpPage() {
  return <main className="help-page" aria-label="Get help" />;
}
